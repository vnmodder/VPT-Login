<Window x:Class="VPT_Login.Views.Test"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VPT_Login.Views"
        mc:Ignorable="d"
        Title="Test" Height="310" Width="428"
         DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBox Text="{Binding Value1.Value, Mode = TwoWay}" Width="200"/>
            <TextBox Text="{Binding Value2.Value, Mode = TwoWay}" Width="200"/>
        </StackPanel>
        <Button Content="Re size" Command="{Binding TestCommand}"/>
        <Button Content="Test" Command="{Binding TestCommand2}"/>
        <TextBlock Text="{Binding LogText.Value, Mode = TwoWay}"/>
    </StackPanel>
</Window>

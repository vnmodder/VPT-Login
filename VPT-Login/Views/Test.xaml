<Window x:Class="VPT_Login.Views.Test"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VPT_Login.Views"
        mc:Ignorable="d"
        Title="Test" Height="310" Width="428"
         DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <!-- phần scroll -->
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBox Text="{Binding Value1.Value}" Width="200"/>
            <TextBox Text="{Binding Value2.Value}" Width="200"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Button Content="Re size" Command="{Binding TestCommand}"/>
            <Button Content="Test" Command="{Binding TestCommand2}"/>
        </StackPanel>

        <TextBox Grid.Row="2"
             Text="{Binding LogText.Value}"
             VerticalScrollBarVisibility="Auto"
             HorizontalScrollBarVisibility="Auto"
             AcceptsReturn="True"
             TextWrapping="Wrap"/>
    </Grid>
</Window>
